<?php

/**
 * Implements hook_mail().
 */
function ibew_contact_mail($key, &$message, $params) {
  switch ($key) {
    case 'contact_form':
      $message['subject'] = $params['subject'];
      $message['body'][] = $params['body'];
      break;
  }
}
